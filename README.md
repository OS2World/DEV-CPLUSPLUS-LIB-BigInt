DEV-CPLUSPLUS-LIB-BigInt
========================

BigInt class. BigInt provides larger integers for the C++ programmer. The bit width can range from 8 to &lt;unlimited>. The library  includes C++ source and a few example programs.
